void character()
{
    clearScreen();
    printf("----------------------------- %sCharacter Description%s ----------------------------\n", GRN, RESET);
    printf("Characters are represented using the char data type, which stores a single\n");
    printf("character using 1 byte of memory. Characters are enclosed in single quotes\n"); 
    printf("(e.g., 'A', '1', '@') and are internally stored as their corresponding\n");
    printf("ASCII values.");
    
    getchar();
}