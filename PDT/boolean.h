void boolean()
{
    clearScreen();
    printf("------------------------------ %sBoolean Description%s -------------------------------\n", GRN, RESET);
    printf("A boolean is a data type used to represent truth values: true (1) or false\n"); 
    printf("(0). C does not have a built-in boolean type in older versions, but starting\n");
    printf("with C99, the stdbool.h header introduces the bool type along with the\n");
    printf("constants true and false");

    getchar();
}